# Rich local PII/contact detection used by the LittleNet web safety layer.
# Deterministic regex rules in safety/pii_service.py remain mandatory fail-safe
# coverage if Presidio or its NLP model is unavailable.
-r requirements-core.txt
presidio-analyzer>=2.2,<3
spacy>=3.8,<4
